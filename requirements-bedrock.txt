# GraphSAST — AWS Bedrock backend
#
# Install alongside core requirements:
#   pip install -r requirements.txt -r requirements-bedrock.txt

anthropic>=0.42.0,<1   # AnthropicBedrock client
boto3>=1.34.0,<2       # AWS credential chain + list_foundation_models
botocore>=1.34.0,<2
